# AGENTS.md

## 项目目标

本仓库用于 AIGC 剧情向图文连载生成。所有 Prompt、模板、配置和工具改动，都必须服务于：
1. 人物形象连续稳定；
2. 场景风格连续稳定；
3. 分镜叙事清晰；
4. 中文文字后期可控；
5. 生成结果可验收、可返工。

## Prompt 生成总规范

本项目所有章节 Prompt 必须由项目级约束层生成或校验，不能只靠单次手写提示词修补。生成顺序固定为：

1. 人物锁定层；
2. 场景锁定层；
3. 画风锁定层；
4. 本帧剧情层；
5. 分镜结构层；
6. 文字策略层；
7. 负面约束层；
8. 验收层。

后续新增章节、返工 Prompt、角色母版 Prompt、场景母版 Prompt，都必须优先读取：
- `config/prompt_policy.json`
- `config/character_lock_chen_nian_lie_gou.json`
- `config/scene_lock_chen_nian_lie_gou.json`
- `config/text_strategy.json`
- `docs/PROMPT_POLICY.md`
- `docs/CHARACTER_LOCK_POLICY.md`
- `docs/CONTENT_SAFETY_PROMPT_GUIDE.md`

## 最高优先级规则

人物锁定 > 场景锁定 > 画风锁定 > 本帧剧情 > 分镜结构 > 文字排版。

如果剧情动作与人物设定冲突，优先保留人物设定。
如果人物动作与盲人行为逻辑冲突，优先保留盲人行为逻辑。
如果文字入图与人物稳定冲突，优先使用空白气泡和后期叠字。
如果剧情原文包含儿童裸露、血腥伤口、击打瞬间、虐打过程、喷血、光身、抽筋、黑帮化、未成年人排泄照护、衣裤/下身细节、近距离身体照料等高风险表达，必须先按 `docs/CONTENT_SAFETY_PROMPT_GUIDE.md` 改写为克制、生活化、保护导向的视觉表达。

## 陈年烈狗人物锁定

### 陶淮南｜童年

必须是 8 岁盲人小孩。

强制视觉表现：
- 大而清亮但不聚焦的黑眼睛；
- 视线不能准确对准人物、碗、冰溜子、门口等目标；
- 经常根据声音转头；
- 用手摸索炕沿、碗、冰溜子确认位置；
- 神情软、敏感、安静；
- 不能画成普通能看见的小孩。

禁止：
- 正常对视；
- 准确盯着物体；
- 成年化；
- 夸张白内障；
- Q版幼儿化。

### 迟苦｜童年

必须是 8 岁左右、瘦、苦、脏、沉默防备的农村小孩。

强制视觉表现：
- 瘦得明显，肩窄，衣服空荡；
- 脸上冻红、伤痕、脏污；
- 破旧脏棉衣；
- 表情防备、沉默、少说话。

禁止：
- 干净漂亮小男孩；
- 现代城市儿童；
- 成年化；
- 情绪夸张表演。

### 陶晓东｜25岁

必须是 25 岁左右的年轻哥哥、家庭顶梁柱、纹身师气质但不是黑帮。

强制视觉表现：
- 黑色半长发，肩膀附近，随手扎成小马尾或小辫子；
- 深色旧厚外套或旧军大衣；
- 高大、肩宽、手掌大；
- 眼神疲惫但温热；
- 外糙内细、可靠护短。

禁止：
- 短发；
- 寸头；
- 油头；
- 韩式男主；
- 黑帮；
- 霸总；
- 潮牌网红。

## 文字策略

默认不直接生成中文文字。
默认生成空白对白气泡和空白矩形旁白框，供后期叠字。
除非输入显式设置 `allow_text_in_image=true`，否则不得把台词、旁白、人物姓名、章节标题写进图中。

即使输入中包含 `story_title`、`phase`、`title`、`dialogue.name` 等字段，也只能作为配置、文案清单或后期叠字素材，不得指示模型把这些文字直接画进图中。

禁止图中出现：
- 陶淮南、迟苦、陶晓东等人物姓名标签；
- 第几章；
- 第几帧；
- 第1幕；
- 当前帧；
- 章节标题；
- 人物介绍浮标。

## 分镜策略

默认 9:16 抖音竖屏。
默认 2-3 个漫画分镜格。
必须有清晰边界。
必须包含：
- 一个空间建立镜头；
- 一个动作推进镜头；
- 一个手部、道具或人物反应特写。

禁止生成单幅海报式大图。

## 返工规则

如果用户指出人物漂移，不要只修改当前 Prompt。
必须判断是否应该更新：
- `config/character_lock_chen_nian_lie_gou.json`
- `config/prompt_policy.json`
- `config/scene_lock_chen_nian_lie_gou.json`
- `config/text_strategy.json`
- `docs/CHARACTER_LOCK_POLICY.md`
- `prompts/templates/frame_prompt_v3.template.md`

如果用户纠正眼睛、服装、年龄、发型、场景年代、场景道具、中文乱码、姓名标签、分镜结构等问题，必须判断是否沉淀到配置或模板。反复出现的问题必须进入项目级规范。

把反复出现的问题沉淀为项目级规范。
