# 通用剧情连载系统 V2.0 — 项目总览

> 生成时间：2026-07-03
> 分支：`owfh-dev` / 基干：`main`

---

## 一、完整目录树

```
aigc-cat-partner/
│
├── 一键批量生成_prompt.bat          # Win 一键批处理
├── .gitignore                       # Git 忽略规则
├── CLAUDE.md                        # 项目 AI 协作指令
├── README.md                        # 仓库说明
│
├── tools/                           # ⚙️ 核心工具（Python 标准库）
│   ├── generate_prompt.py           #   剧情提示词流水线生成器
│   ├── generate_batch_plan.py       #   批量运行器
│   ├── extract_chapter_info.py      #   可选：从正文抽取人物/场景/情绪
│   └── validate_config.py           #   可选：检查 JSON 配置完整性
│
├── config/                          # 📋 配置定义
│   ├── series_input_schema.json     #   通用输入 schema（主线）
│   ├── minimal_input_schema.json    #   最小输入 schema
│   ├── story_templates.json         #   故事模板 & 帧节拍
│   ├── character_spec.json          #   角色视觉基因模板
│   ├── world_spec.json              #   世界 / 风格模板
│   ├── scenes.json                  #   场景配置
│   ├── reference_manifest.json      #   跨会话身份/风格引用清单
│   ├── negative_prompt_common.txt   #   通用负向提示词
│   ├── negative_prompt_project.txt  #   项目特定负向提示词（占位）
│   └── negative_prompt.txt          #   负向提示词汇总
│
├── examples/                        # 📝 输入样例
│   ├── generic_suspense_series.sample.json
│   ├── character_image.sample.json  #   角色立绘样例
│   ├── chapter_scene.sample.json    #   场景构图样例
│   └── batch_plan.sample.json       #   批量计划样例
│
├── prompts/                         # 🎯 生成出来的提示词（按用途分类）
│   ├── character/
│   ├── scene/
│   ├── chapter/
│   └── batch/
│
├── outputs/                         # 📦 最终产物
│   ├── images/
│   ├── markdown/
│   ├── batch_plans/                 #   generate_prompt.py 输出目录
│   └── logs/
│
├── references/                      # 🖼️ 参考图与母版图
│   ├── style/
│   ├── character_master/
│   ├── scene_style/
│   └── composition/
│
├── 入口/                            # 🚪 快速入门
│   └── START_HERE.md
│
└── 00_设定文档/                     # 📚 设定与设计
    ├── 通用剧情连载系统_沉淀复用实施方案.md   # 架构设计
    ├── 人物设定_陈年烈狗.md                   # 角色设定
    ├── 人物出场表_逐章分析.md                  # 出场表
    ├── 场景设定_陈年烈狗.md                    # 场景设定
    └── 原文/                                  # 正文 1~126 章
        ├── 00_header.txt              (卷首)
        ├── 第1章.txt  ~  第126章.txt  (正文)
```

---

## 二、核心流水线

### 2.1 流水线总图

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ minimal  │───→│ ❶ story_plan │───→│ ❷ frame_plan │───→│ ❸ copy_plan │
│ input    │    │ (故事规划)    │    │ (分帧规划)    │    │ (文案规划)    │
└──────────┘    └──────────────┘    └──────────────┘    └──────────────┘
                                                               │
                                                               ▼
                    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
                    │ ❻ retake    │←───│ ❺ qa_result │←───│ ❹ prompt    │
                    │ _prompt     │    │ (QA 验收)    │    │ _plan       │
                    │ (返工提示)   │    │               │    │ (提示词规划) │
                    └──────────────┘    └──────────────┘    └──────────────┘
                           │                                        │
                           ▼                                        ▼
                    ┌──────────────────────────────────────────────────────┐
                    │             prompt_task.md                          │
                    │   (完整生成任务文档，含验证清单 + 发布顺序)           │
                    └──────────────────────────────────────────────────────┘
```

### 2.2 函数级调用链

```
main()
├── load_json(args.input)                 # 读取输入 JSON
├── validate_input(data)                  # 校验必填字段
├── normalize_defaults(data)              # 填充默认值
├── build_prompt_bundle(data)
│   ├── frame_plan(data)                  # → list[dict]  分帧规划
│   ├── build_prompt(data, frame)         # → str         逐帧生成 Prompt 文本
│   └── build_pipeline_artifacts(data, frames, prompts)    # 打包 6 个产物
│       ├── build_story_plan(data)        # → dict         ❶ 故事规划
│       ├── build_frame_plan(data,frames) # → list[dict]   ❷ 结构化帧规划
│       ├── build_copy_plan(data,frames)  # → list[dict]   ❸ 文案规划
│       ├── build_prompt_plan(data,frames,prompts) → list[dict] ❹ 提示词规划
│       ├── build_qa_result(data,frames)  # → list[dict]   ❺ QA 验收
│       └── build_retake_prompt(data,frames) → list[dict]  ❻ 返工提示
└── write_task(data, bundle, prompts)     # → 写入 outputs/batch_plans/
```

### 2.3 执行命令

```bash
# 单任务
python tools/generate_prompt.py --input examples/generic_suspense_series.sample.json

# 命令行参数覆盖（可部分覆盖 JSON 字段）
python tools/generate_prompt.py --input examples/generic_suspense_series.sample.json --image-count 4 --scene "雨中车站"

# 批量生成（逐个串行调用，无并发）
python tools/generate_batch_plan.py --input examples/batch_plan.sample.json

# 可选：验证配置文件完整性
python tools/validate_config.py

# 可选：从正文抽取信息
python tools/extract_chapter_info.py --chapter 第1章
```

---

## 三、文件分类统计

| 类别 | 目录 | 文件数 | 说明 |
|---|---|---|---|
| ⚙️ 核心工具 | `tools/` | **4** | 2 个必用 + 2 个可选 |
| 📋 配置 | `config/` | **10** | JSON schema、模板、提示词 |
| 📝 样例 | `examples/` | **4** | 含剧情、角色、场景、批量 |
| 🎯 生成提示词 | `prompts/` | — | 空目录（按需使用） |
| 📦 最终产物 | `outputs/` | — | 代码输出 + 手动存放 |
| 🖼️ 参考图 | `references/` | — | 空目录（按需使用） |
| 🚪 入口 | `入口/` | **1** | 快速入门文档 |
| 📚 设定文档 | `00_设定文档/` | **4** | 架构设计、人物、场景 |
| 📄 正文原文 | `00_设定文档/原文/` | **127** | 含 header + 126 章正文 |
| 📄 根目录 | `./` | **4** | CLAUDE.md, README 等 |

---

## 四、架构设计要点

### 4.1 设计哲学

```
最小输入 ──→ 文本制品（可审查、可修复）──→ 图像生成
```

所有工作流优先产出结构化 JSON 文本，经过人工/自动 QA 后再进入生成环节。

### 4.2 两种生成模式

| 模式 | `continuous_story` | 帧来源 | 帧间承接 | 适用场景 |
|---|---|---|---|---|
| **连续故事** | `"是"` | `story_templates.json` 帧循环 | 有承接，角色/道具/情绪逐帧推进 | 连载剧情 |
| **同主题变体** | `"否"` | 角度轮转（5 种角度） | 不强制剧情承接 | 主题散图/变体 |

**帧循环规则**（连续模式）：当 `image_count > len(template.frames)` 时，帧按 `frames[i % len(frames)]` 循环复用，`story_phase` 自动递增编号（第 1 幕、第 2 幕…）。每帧自动推算 `prev_phase`（上一帧阶段）和 `next_hint`（下一帧提示）。

### 4.3 锚帧引用规则（核心）

| 帧位置 | `generation_type` | `required_references` | `role` |
|---|---|---|---|
| Frame 1 | `anchor` | 身份参考图 + 风格参考图 + 场景母版图 | `anchor` |
| Frame 2 | `continuation` | 身份参考图 + 风格参考图 + **第 1 张锚点图** + 场景母版图 | `continuation` |
| Frame 3+ | `continuation` | 身份参考图 + 风格参考图 + 第 1 张锚点图 + **上一张成图** + 场景母版图 | `continuation` |
| 非连续 | `variant` | 身份参考图 + 风格参考图 + 场景母版图 | `variant` |

**引用优先级**：身份参考图 / 角色设定 > 风格参考图 / 世界观设定 > 第 1 张锚点图 > 上一张成图 > 当前 Prompt。

### 4.4 QA → Retake 反馈环

```
qa_result.json       ← 每帧状态（待检查 / 通过 / 不通过）+ 失败项 + 失败原因
                            │
                            ▼ 不通过
retake_prompt.json   ← 自动生成返工 Prompt + 必须保留的参考图清单
                            │
                            ▼
               重新生成（保留参考图，不重新设计角色/服装/背景）
```

内置 8 个通用验收项：角色一致、风格一致、世界观一致、造型道具连续、动作清楚、情绪推进、文字安全、平台适配。

### 4.5 默认值解析顺序

```
命令行 --key value （最高优先级）
        ↓
JSON 输入文件中的值
        ↓
series_input_schema.json 中 defaults 块
        ↓
normalize_defaults() 中硬编码兜底（最低优先级）
```

### 4.6 负向提示词分离

```
negative_prompt_common.txt    ← 通用负向（跨项目共享，处理肢体错误、画质问题等）
negative_prompt_project.txt   ← 项目特定（占位，可定制角色/世界观/风格禁忌）
project_negative_prompt       ← 行内输入（在 JSON 或命令行中临时传入）
        ↓
negative_prompt() 合并结果    ← common + project + inline，三者叠加
```

### 4.7 文字渲染策略

默认「生成空白气泡 + 后期叠字图层」，支持 4 种模式：

| `text_render_mode` | 行为 |
|---|---|
| 仅生成文案清单 | 画面中无文字，仅输出文案 |
| 生成空白气泡 + 文案清单 | 画面保留气泡位置，文字后期添加 |
| **生成空白气泡 + 后期叠字图层（默认）** | 气泡 + 文字图层分开处理 |
| 直接在图中生成文字 | 短文字直接渲染，需避免乱码 |

---

## 五、配置体系详解

### 5.1 配置解析时机

| 配置文件 | 解析函数 | 作用 |
|---|---|---|
| `series_input_schema.json` | `validate_input()` + `normalize_defaults()` | 定义必填字段、默认值、字段描述 |
| `story_templates.json` | `choose_story_template()` → `template_frames()` | 按模板名获取帧序列 + 情绪曲线 |
| `character_spec.json` | 仅人工填写 | 角色视觉基因模板（运行时不读取） |
| `world_spec.json` | `build_prompt()` 中作兜底 | 当输入缺少 `visual_gene`/`worldview`/`style` 时取此文件值 |
| `scenes.json` | 仅人工填写 | 场景配置数据 |
| `reference_manifest.json` | 仅人工填写 | 跨会话引用清单 |
| `negative_prompt_common.txt` | `negative_prompt()` | 通用负向提示词 |
| `negative_prompt_project.txt` | `negative_prompt()` | 项目特定负向提示词 |
| `negative_prompt.txt` | 人工参考 | 汇总文件 |

### 5.2 内置故事模板

| 模板名 | 情绪曲线 | 帧数 | 适用题材 |
|---|---|---|---|
| `通用悬念连载` | 平静→疑惑→试探→紧张→反转→余味 | 6 | 悬疑/推理/惊悚 |
| `日常反转连载` | 轻松→好奇→误会→尴尬→反转→松弛 | 6 | 日常/轻喜剧/反转 |

新增模板方式：在 `story_templates.json` 中添加新 key 和对应结构；或在输入中使用 `custom_frames` 数组完全覆盖。

### 5.3 必填 & 高频字段一览

**必填（4 个）**：`scene`、`image_count`（1-12）、`continuous_story`、`story_template`

| 字段 | 说明 | 默认值 |
|---|---|---|
| `story_outline` | 完整故事线（不要塞进 extra） | `""` |
| `visual_gene_summary` | 角色视觉特征描述 | `world_spec.json` 兜底 |
| `worldview_summary` | 世界观边界 | `world_spec.json` 兜底 |
| `style_prompt` | 目标画风 | `world_spec.json` 兜底 |
| `emotional_progression` | 情绪推进逻辑说明 | `""` |
| `extra` | 补充约束 / 禁忌项 | `""` |
| `mood_curve` | 自定义情绪曲线（数组） | 模板自带 |
| `project_negative_prompt` | 行内项目负向词 | `""` |
| `bubble_mode` | 无 / 单角色气泡 / 多角色气泡 / 旁白标题 | `多角色气泡` |
| `text_render_mode` | 见 4.7 节 | `生成空白气泡 + 后期叠字图层` |
| `interaction_mode` | 不互动 / 看向观众 / 提问互动 / 评论引导 / 点赞关注 / 选择投票 | `评论引导` |
| `outfit_style_lock` | 不锁定 / 弱锁定 / 强锁定 | 连续故事→`强锁定` |
| `in_image_storyboard` | 关闭 / 开启 | `关闭` |
| `ratio` | 9:16 / 4:3 | `9:16` |
| `platform` | 通用 / 抖音竖屏 / 小红书图文 | `抖音竖屏` |

---

## 六、输出产物详解

### 6.1 输出目录结构

```
outputs/batch_plans/
└── {timestamp}_{scene}_{比例}_{张数}_V2_pipeline/
    ├── prompt_task.md        # 完整生成任务文档（含验证清单 + 发布顺序）
    ├── input.json            # 标准化入参
    ├── story_plan.json       # ❶ 故事规划
    ├── frame_plan.json       # ❷ 分帧规划
    ├── copy_plan.json        # ❸ 文案规划
    ├── prompt_plan.json      # ❹ 提示词规划
    ├── qa_result.json        # ❺ QA 验收
    └── retake_prompt.json    # ❻ 返工提示
```

### 6.2 六阶段产物结构

| 产物 | 关键字段 |
|---|---|
| **story_plan** | `story_id`, `story_title`, `story_template`, `story_outline`, `mood_curve[]`, `image_count`, `continuous_story`, `anchor_frame_index`, `text_render_mode` |
| **frame_plan** (每帧) | `frame_index`, `main_action`, `continuity_from_previous`, `visual_evidence[]`, `character_emotion`, `subject_state`, `camera`, `role`(anchor/continuation/variant), `source_scene` |
| **copy_plan** (每帧) | `title`, `narration`, `character_lines[]`(name+text), `comment_prompt`, `text_position`(title/character_lines/comment_prompt) |
| **prompt_plan** (每帧) | `generation_type`, `required_references[]`, `reference_rule`, `positive_prompt`, `negative_prompt`(common+project) |
| **qa_result** (每帧) | `status`(待检查/通过/不通过), `required_checks[]`, `failed_items[]`, `reason`, `next_action` |
| **retake_prompt** (每帧) | `trigger`(失败项), `retake_prompt`(含必须保留参考图的指令), `references_to_keep[]` |

---

## 七、批量系统

`tools/generate_batch_plan.py`（仅 33 行）的工作方式：

```
batch_plan.json 中的 tasks[]
        │ (逐个串行)
        ▼
写入临时 JSON (outputs/batch_plans/_tmp_batch_inputs/)
    → subprocess.run(generate_prompt.py)
    → 写入临时 JSON → ...
```

无并行、无异步，按顺序逐个生成。适合夜间批量跑多个故事线。

```json
// batch_plan.sample.json 的结构
{
  "tasks": [
    { "scene": "...", "image_count": 6, "story_template": "通用悬念连载", ... },
    { "scene": "...", "image_count": 4, "story_template": "日常反转连载", ... }
  ]
}
```

---

## 八、可选工具

| 工具 | 命令 | 用途 |
|---|---|---|
| `validate_config.py` | `python tools/validate_config.py` | 检查 JSON 配置是否完整、可解析 |
| `validate_config.py` | `python tools/validate_config.py config/story_templates.json` | 指定文件检查 |
| `extract_chapter_info.py` | `python tools/extract_chapter_info.py --chapter 第1章` | 从正文中抽取人物/场景线索 |

---

## 九、核心规则

1. **输出格式稳定** — 六阶段产物 schema 不可随意改动
2. **禁止并行生成** — 连续故事必须逐帧生成，不能一次性把所有 prompt 发给图片模型
3. **坚持锚帧顺序** — Frame 2 必须引用 Frame 1 锚点，Frame 3+ 必须引用 Frame 1 + 上一帧
4. **空气泡渲染** — 默认空白气泡 + 后期叠字，避免图片模型直接生成中文乱码
5. **负向提示词分离** — `common` + `project` 分开维护
6. **IP 隔离** — 通用层不携带旧 IP 内容（人猫搭档、外卖故事等）
7. **不删历史资产** — 旧项目文件保留为历史参考
8. **不加新依赖** — 只用 Python 标准库
9. **小步可逆修改** — 改动 `generate_prompt.py` 后必须用两个 sample 做 P0 验证

---

## 十、git 状态摘要

| 项目 | 内容 |
|---|---|
| 当前分支 | `owfh-dev` |
| 最新提交 | `13974ff` — refactor: 迁移至通用剧情连载系统 V2.0，移除旧 IP 专属内容 |
| 未暂存变化 | 新目录 `prompts/`、`outputs/`、`references/` + 新工具 + 新样例 |
